<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="ManagedTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Background" Value="{DynamicResource ManagedBrushBgSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ManagedBrushBorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource ManagedFontFamily}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextPrimary}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ManagedInputFocusVisualStyle}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="ManagedPasswordBoxStyle" TargetType="{x:Type PasswordBox}">
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Background" Value="{DynamicResource ManagedBrushBgSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ManagedBrushBorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource ManagedFontFamily}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextPrimary}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ManagedInputFocusVisualStyle}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="ManagedCheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextPrimary}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ManagedButtonFocusVisualStyle}" />
    </Style>

    <Style x:Key="ManagedComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="MinWidth" Value="300" />
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource ManagedBrushBgSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ManagedBrushBorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ManagedInputFocusVisualStyle}" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ComboBoxItem}">
                    <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextPrimary}" />
                    <Setter Property="Background" Value="{DynamicResource ManagedBrushBgSurface}" />
                    <Setter Property="Padding" Value="12,8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                <Border
                                    x:Name="ItemBorder"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ManagedBrushBgBubbleHover}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ManagedBrushPrimary100}" />
                                        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushSecondaryButtonText}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton
                            Background="Transparent"
                            BorderThickness="0"
                            Focusable="False"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            VerticalAlignment="Stretch"
                            Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <ContentPresenter
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="Center" />
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource ManagedRadiusSmall}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        TextElement.Foreground="{TemplateBinding Foreground}" />
                                    <Path
                                        Grid.Column="1"
                                        Width="8"
                                        Height="5"
                                        Margin="12,0,0,0"
                                        Data="M 0,0 L 8,0 L 4,5 Z"
                                        Fill="{DynamicResource ManagedBrushTextSecondary}" />
                                </Grid>
                            </Border>
                        </ToggleButton>
                        <Popup
                            x:Name="PART_Popup"
                            AllowsTransparency="True"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            PopupAnimation="Fade">
                            <Border
                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                Background="{DynamicResource ManagedBrushBgSurface}"
                                BorderBrush="{DynamicResource ManagedBrushBorderDefault}"
                                BorderThickness="1"
                                CornerRadius="{DynamicResource ManagedRadiusSmall}">
                                <ScrollViewer MaxHeight="240" CanContentScroll="True">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
