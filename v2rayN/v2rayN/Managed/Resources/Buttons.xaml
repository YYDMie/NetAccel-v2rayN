<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="ManagedPillButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="{DynamicResource ManagedControlPadding}" />
        <Setter Property="Background" Value="{DynamicResource ManagedBrushPrimaryButton}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ManagedBrushPrimaryButton}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushPrimaryButtonText}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource ManagedButtonFocusVisualStyle}" />
        <Setter Property="FontFamily" Value="{DynamicResource ManagedFontFamily}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="ButtonBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource ManagedRadiusPill}">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ManagedBrushPrimaryButtonHover}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ManagedBrushPrimaryButtonHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ManagedBrushInfo}" />
                            <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ManagedBrushBorderDefault}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ManagedBrushBorderDefault}" />
                            <Setter Property="Foreground" Value="{DynamicResource ManagedBrushTextDisabled}" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="ManagedSecondaryPillButtonStyle"
        BasedOn="{StaticResource ManagedPillButtonStyle}"
        TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource ManagedBrushPrimary100}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ManagedBrushPrimary100}" />
        <Setter Property="Foreground" Value="{DynamicResource ManagedBrushSecondaryButtonText}" />
    </Style>
</ResourceDictionary>
