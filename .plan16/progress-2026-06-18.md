# Plan 16 Progress - 2026-06-18

## 当前结论

计划 16 共 125 个任务项。按权威任务树和现有验收记录统计：

- WP-09 的 5 项本地技术任务已于 2026-06-19 通过收口验收。
- T16-R3-17 文档和 UI 技术证据完成，真实登录、连接、切线和退出恢复并入 R5 实机闭环。

代码层面已覆盖约 66%，但 R4 运行闭环、更新供应链和 R5 发布候选验收仍是主要余量。

## 已验收主线

- R0：fork、应用身份、原生基线和运行时隔离 PoC，整体已验收。
- R1：WP-02A 至 WP-02D 的 Master 契约与服务端闭环，以及 WP-03 客户端身份和启动链，已验收。
- R2：WP-04 托管模型/适配器、WP-06 双来源隔离、WP-10 稳定加密和缓存同步，已验收。

## 已完成代码、等待收口

- WP-05：连接协调、授权集合内选线、安全切线和 fallback 已通过代码复核；真实系统代理、TUN、线路和双模式 E2E 仍需实机证据。
- WP-07A 至 WP-07D：设计令牌、托管壳、登录、首页和线路页均为 `code_complete`，仍需真实 Master、缩放、键盘和默认启动交接证据。
- WP-08A：记录页为 `code_complete`。
- WP-08B：诊断检查和可逆修复为 `code_complete`，仍需真实 Windows 故障注入。
- WP-08C：allowlist 诊断导出为 `code_complete`，仍需真实 WPF 保存对话框和 ZIP 人工检查。
- WP-08D：设置、单一托盘、默认托管入口和经典窗口安全交接为 `code_complete`，仍需真实 Windows 托盘、关闭隐藏、经典打开/返回和多缩放证据。

## WP-08C 本轮交付

- WPF 增加“导出诊断包”入口和友好成功、取消、失败状态。
- ZIP 严格只生成 `diagnostics.json` 与 `privacy.txt`。
- 导出不调用托管配置提供器，不读取线路参数、日志、SQLite、envelope、运行时配置或账号凭据。
- 状态 JSON 不包含 profile ID、display name、endpoint、原始错误消息、token、credential 或日志正文。
- 使用同目录临时文件写入；失败后清理临时包，不留下部分产物。
- 注入 token、UUID、endpoint、profile ID、Authorization 和文件写入失败场景进行测试。

验证结果：

- `ManagedDiagnosticsServiceTests`：12/12 通过。
- `NetAccel.Managed.Tests`：269/269 通过。
- WPF Debug 构建：0 warning，0 error。
- `git diff --check`：通过。
- 导出边界扫描：未发现日志、数据库、配置目录读取或目录整体压缩路径。

## 后续任务

- WP-09：代码、图标、主题、无障碍和真实 WPF 自动化已完成；仅保留 R5 外部环境证据。
- WP-11：会话、outbox、质量指标、稳定事件码、管理闭环和紧急控制，共 7 项。
- WP-12：除诊断包 allowlist 外的更新、验签、回滚、SBOM 和安全门禁，共 6 项；连同 WP-11，R4 尚未实现 13 项。
- WP-13 与 WP-14：R5 全部 24 项真实环境、稳定性、安全、更新和 RC 验收。

## 下一步顺序

1. 补齐 WP-07/WP-08 的真实 Windows、多缩放、托盘和经典切换证据并验收 R3。
2. 在 R5 受控账号与真实线路环境执行 WP-09 新手走查的网络闭环部分。
3. 进入 WP-11 会话和控制闭环，再执行 WP-12 更新与供应链。
4. 集中完成 WP-05 遗留实机证据及 WP-13/WP-14 的 R5 发布候选验收。
